import {POST_VOTES} from '../actions/types';

const intialState = {
  item: [],
  items: {}
}

export default function (state = intialState, action) {
  switch(action.type) {
    default: 
      return state;
  }
}